use math
print("math: " + str(keys(math)))
use collections
print("collections: " + str(keys(collections)))
use map
print("map: " + str(keys(map)))
use json
print("json: " + str(keys(json)))
use csv
print("csv: " + str(keys(csv)))
use regex
print("regex: " + str(keys(regex)))
use time
print("time: " + str(keys(time)))
use error
print("error: " + str(keys(error)))
use net
print("net: " + str(keys(net)))
use crypto
print("crypto: " + str(keys(crypto)))
use log
print("log: " + str(keys(log)))
use http
print("http: " + str(keys(http)))
